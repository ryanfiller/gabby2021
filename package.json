{
  "name": "@example/starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "surge": "npm run build && surge --project dist --domain beta.gabbywindham.design"
  },
  "devDependencies": {
    "astro": "^0.20.5"
  }
}
