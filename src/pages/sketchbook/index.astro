---
import PageLayout from '../../layouts/page.astro'

import SketchbookGrid from '../../components/sketchbook-grid.astro';

import { sortByDates } from '../../helpers/index.js'

const pages = Astro.fetchContent('.//*.md').sort((a, b) => sortByDates(a, b));
---

<PageLayout title="About">
    sketchbook index page

    <SketchbookGrid items={pages} />
</PageLayout>
